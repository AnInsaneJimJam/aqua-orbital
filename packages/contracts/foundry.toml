[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["vendor"]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 700
via_ir = true
evm_version = "cancun"
fuzz.runs = 256
invariant.runs = 32
invariant.depth = 64

[profile.ci]
fuzz.runs = 2048
invariant.runs = 256
invariant.depth = 128

[profile.release]
fuzz.runs = 10000
invariant.runs = 1024
invariant.depth = 256

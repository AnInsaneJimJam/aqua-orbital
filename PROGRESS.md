# Orbital progress

Updated 2026-09-09. Status records observed implementation, never planned success.

| Gate | Status | Next evidence |
| --- | --- | --- |
| Documentation | Implemented; maintained during build | Master reconciled; focused guides and evidence map created |
| G0 scaffold | Complete locally; project Arc identity verified | Frozen toolchain, pinned/licensed sources, interfaces, Docker/PostgreSQL/Cancun Anvil verified; project-deployed upstream AquaRouter and Orbital runtimes/receipts now verified on Arc |
| G1 mathematics | In progress; acceptance blocked | Certified composition executes in the Router; 128/128 independent mixed pilot actions pass. Equality/discovery liveness, wider economic campaigns and full supported-range verification remain open |
| G2 SwapVM extension | In progress; depends on G1 | Actual 0x72 fee and interior/mixed 0x52 execution pass local integration tests; complete release campaigns and target gas acceptance outstanding |
| G3 Aqua settlement | In progress; depends on G2 | Official Aqua lifecycle, exact transfer deltas, all-token backing and real six-pair execution pass; full security/release campaigns outstanding |
| G4 Arc payments | In progress | Local interior and mixed swaps fund split USDC invoices; mined mixed failure/rollback and recovery verified. First actual Arc swap-funded invoice receipt verified; Privy wallet association and full PAY campaign outstanding |
| G5 SDK/backend | In progress | Validated plans, compiled event ABIs, canonical projections, receipt metrics, invoice/strategy reads, public swap/payment observations and bounded quote cache; wallet flows and separate incomplete-shipment pagination connected locally. Transient database connection recovery verified with readiness failing closed; full rebuild/release campaigns outstanding |
| G6 application | In progress | All routes scaffolded, editable presentation/controller split, public invoice reads/recovery, Privy login UI verified and external fixture browser tests. Explicit embedded-wallet creation, connector selection and recoverable onboarding errors implemented. Payment, standalone swap and invoice administration review/recovery implemented; publication, nonce-based replacements, funding, invoice history, registered/incomplete liquidity and owner administration connected; same-action repricing tracking implemented. Live embedded-wallet creation/execution and broader recovery checks outstanding |
| G7 demo | Local integration demonstrated; Arc strategy, swap and invoice confirmed | Deployment, activation, the user's 1-USDC → 0.998491-oUSD6 swap and a 0.5-USDC invoice funded from oUSD6 are receipt-confirmed. Exact swap transfer legs, recipient payout and payer refund verified. Authenticated Privy wallet association remains open |
| G8 release | Not started | Full campaigns, limits, gas, accessibility/performance |

## Environment and external dependencies

- Initial inspection: Node 22.18.0, Python 3.12, Foundry 1.5.1 available.
- Initial global pnpm shim attempted unavailable pnpm 12.3.4. Repaired by pinning project-local pnpm 10.34.5 and retaining the exact lockfile.
- Docker engine was initially unavailable. Docker Desktop was started; PostgreSQL and Cancun Anvil now run. The isolated PostgreSQL integration test and Docker API/web startup checks pass. Temporary app test containers were stopped; the main production preview and host API remain available.
- Arc RPC/chain ID/USDC decimals were observed. The canonical Aqua address had **empty runtime code** at the recorded block. The owner authorized a separate project deployment of unchanged upstream AquaRouter, which has now passed receipt/runtime activation. Canonical availability and sponsor acceptance remain separate. See [deployment history](docs/ARC_DEPLOYMENT_STATUS.md) and [actual verification](deployments/5042002/verification.json).
- Earlier read-only checks at Arc block 61,024,262 found empty code at both published Aqua/stock-router addresses. Hash-pinned reads and transient-storage simulation work; the observed block gas limit is 30M. A possible Osaka per-transaction limit is recorded as conditional, not verified. See [Arc observations](docs/ARC_DEPLOYMENT_STATUS.md); no transaction was submitted.
- The earlier September 9 read-only observation at block 61,123,941 found no code at the checked Aqua/stock-router addresses. The supplied public address `0x5eBA55e1b43c8714E4432250Dada7A518780C871` then held 20 testnet USDC with nonce 0; wallet control and total budget were unverified at that checkpoint. The twelve subsequent user-signed deployment receipts supersede those preparation limitations, but do not authenticate an official canonical Aqua deployment. [Historical pinned results](deployments/5042002/research-2026-09-09.json).
- Owner supplied public Privy app ID `cmtrvczqp00qh0cjv4b16j1ag`; configured in ignored apps/web/.env.local. The new `dev:arc` profile uses web port 3002 and API port 3003 with real Privy and fixture wallets off. Its page and public email/wallet modal loaded successfully in a fresh browser context without SDK errors; no email, authentication or signature was submitted. No authenticated embedded wallet, signing or reconnection has been verified. [Profile](docs/ARC_DEMO.md), [public-modal observation](test/evidence/arc-integration/privy-login.json).
- Earlier local checkpoints performed no Arc writes. The authorized Arc deployment completed twelve successful user-signed transactions and passed runtime/binding activation. Maker faucet claims, first strategy activation, trader swap and swap-funded invoice are now receipt-confirmed on Arc. Privy wallet-provider association remains unverified. Anvil receipts remain separate local evidence.
- During an earlier local increment Docker's engine stopped while its app/proxy remained running. Restarting Docker Desktop recovered the existing PostgreSQL/Anvil containers and preserved the database volume. Anvil's in-memory state restarted; no Arc deployment was configured.

## Working log

- Frontend refinement complete: unified typography, styled menus/dialogs, responsive layouts, receipt links and button hover contrast. Demo tokens and Protocol notes are in the main navigation; the supplied SVG uses cream/chartreuse with unchanged geometry. Funding and relevant transaction reviews retain contextual disclosures. Protocol notes links verified Arc identity, live receipts, Privy UI and shared-flow smoke records; the obsolete hardcoded Arc-unavailable summary is corrected. Web typecheck, nine focused UI/transaction cases and a navigation/brand/evidence check passed; desktop/320px/390px layouts inspected. Transaction construction, SDK and contracts are unchanged. [Verification and retained limitations](test/evidence/frontend-polish/README.md).

- Replaced the required editable invoice input ceiling with automatic, read-only payment calculation. Selecting a token requests a validated quote bounded by the current wallet balance; advanced options allow a lower spending limit. Approval remains for the exact quoted input, followed by a fresh separate payment review. Two helper checks, four focused browser cases and the web typecheck passed; desktop/mobile review screenshots inspected. No backend, contract, deployment or environment change. [Light evidence](test/evidence/payment-automatic/README.md).

- First actual Arc swap-funded invoice confirmed: transaction `0xb785144b6eb1ed84de359553602c8a3b6e429a6303f3312294f5961576b01877`, block **61,149,658**, payer `0x954ACE1023Cdb3798F69582bbdDcBAcd878B73A9`. **0.5 oUSD6** produced **0.500507 USDC**, paid merchant `0x5eBA55e1b43c8714E4432250Dada7A518780C871` **0.5 USDC**, and refunded **0.000507 USDC** to the payer. Curve fee **0.00025 oUSD6**, no crossings, strategy version 3. Verified canonical success, invoice/swap events and exact USDC payout/refund transfers read-only. [Actual receipt](test/evidence/arc-integration/first-payment.json). This user-signed transaction predates the automatic-input UI change; wallet provider remains unverified.

- First actual Arc swap confirmed: transaction `0xc27397b2bb8557175cc1a824e501046eb4c1373fe396f437c7a0b6737c488e56`, block **61,147,486**, trader `0x954ACE1023Cdb3798F69582bbdDcBAcd878B73A9`. Receipt shows **1 USDC → 0.998491 oUSD6**, with **0.0005 USDC** input fee, no tick crossings and strategy version 2. Independently matched canonical success, router event and both USDC input legs plus demo-token output. [Actual receipt](test/evidence/arc-integration/first-swap.json). The wallet provider cannot be established from this chain receipt. Next: a swap-funded invoice; the trader can use its received oUSD6, avoiding another demo-token claim.

- Repaired quote/review timing: API admission still checks ten-second index age and twenty-second quote expiry, while immutable wallet reviews now receive up to sixty seconds after live preflight. Submit independently rechecks unchanged terms, exact simulation, funding, state and gas bounds. Swap-funded payments use a local three-minute deadline capped by invoice expiry. Public estimates refresh earlier and recover automatically; background refresh no longer disables Review, and invoice polling no longer discards payment reviews. [Focused timing checkpoint](test/evidence/quote-refresh/README.md). Actual user signatures and receipt verification remain the next live steps.

- Confirmed the user's first Arc strategy activation at block **61,144,147**, including sender, router, decoded configuration, canonical receipt and event. Indexed detail shows active version 1, **10 USDC + 10 oUSD6 + 10 oUSD18**, **0.05% fee**, and all allocations live/backed. One read-only quote with a synthetic non-maker caller returned **0.998491 oUSD6 for 1 USDC**. This is quote evidence, not a trader transaction or Privy authentication. [Receipt and scope](test/evidence/arc-integration/first-strategy.md). Next: a different user-chosen wallet executes a refreshed swap, then the swap-funded invoice walkthrough.

- Fixed the browser-specific `ERR_BLOCKED_BY_CLIENT` case by moving Arc app reads/simulations to a bounded same-origin `/api/chain` route using the verified provider. It cannot sign/broadcast or accept arbitrary upstream URLs/state overrides. Guarded duplicate connection/selection requests, selected Coinbase EOA support for Arc, and scoped extension-attribute hydration suppression to the root body. Two relay checks, final web typecheck and two live-origin Chromium checks passed with Blockdaemon deliberately blocked; no unexpected browser errors occurred. Actual competing extensions/pending requests still require browser-side resolution. [Evidence](test/evidence/browser-rpc/README.md).

- Both user-submitted faucet transactions were confirmed independently against their sender, target, faucet calldata, canonical successful receipt and mint event. Maker balances observed: **1,000 oUSD6 + 1,000 oUSD18**. No agent signed or submitted these transactions. [Actual funding receipts](test/evidence/browser-rpc/faucet-receipts.json). Next: publish the first Balanced/0.05% strategy with 10 units per token, then trade/pay from a different chosen wallet.

- Live Arc indexer caught up after the bounded 64-block worker restart. `/ready` returned `200 READ_DEPENDENCIES_READY` twice in the recorded observation, with one later `INDEXER_CHANGED` response during normal concurrent advancement. `/strategies` returned available with complete 7,029-block coverage and no strategies; `/quotes/swap` returned an observed empty route set, accurately reflecting the missing first liquidity publication. [Catch-up observations](test/evidence/arc-integration/live-indexer.json), [actual public API reads](test/evidence/arc-integration/live-public-reads.json). Next: maker claims demo tokens and publishes 10 units of each asset with Balanced/0.05%, then a different chosen wallet executes the swap/payment walkthrough.

- Reproduced primary Arc RPC rate limiting and selected the officially listed Blockdaemon public endpoint. Full deployment verification passed through the selected provider before the Arc profile derived its runtime manifest. Browser/API configuration agrees, and the funding balance/getter/simulation/fee/recheck pattern passes; no faucet transaction was submitted by these probes. [Activation and RPC recovery evidence](test/evidence/arc-integration/deployment-and-rpc.md).

- Focused final indexing increment: six range/ordered/reorg tests and DB/indexer typechecks passed. One isolated real Arc window processed 64 blocks in 4.334 seconds; the live worker subsequently caught up as recorded above. Funding and strategy receipt recovery now tolerate only a verified RPC URL change while retaining every other deployment field; its focused regression and web typecheck passed. Completed-deployment startup also accepts unrelated pending maker transactions after all deployment receipts authenticate. [Range evidence](apps/indexer/test/range-throughput.md), [receipt recovery evidence](test/evidence/arc-integration/provider-recovery.md). Broad campaigns remain deferred.

- Arc deployment completed: twelve successful transactions from the authorized user wallet, starting at block 61,131,616. Actual deployment gas cost totaled approximately 0.602264 testnet USDC. Verified AquaRouter `0xE60f79571E7EDba477ff98BAdeE618b5605DF7aE`, Orbital router `0x449420E9042c48Eac6E695020613678aD5A55D41`, and payments `0xf64e4664D534AeA5d240e1E29DAE9E80D2e393d6`; [all receipts and bindings](deployments/5042002/verification.json). Activation initially exposed a verifier mistake: upstream `multicall(bytes[])` has no outputs, while our check expected an empty array. Corrected the check to the actual pinned ABI and completed activation; no redeployment was needed.

- At the first restart, landing, swap, funding and liquidity setup returned HTTP 200, loaded actual Arc addresses and showed no missing-deployment or fixture-wallet UI; public Privy email/external login opened without browser errors. [Browser observation](test/evidence/arc-integration/deployed-ui.json). That checkpoint encountered indexer RPC errors, subsequently resolved by the provider and throughput changes above. Wallet creation/authentication and financial-flow receipts remain separate unfinished verification.

- Initial bounded indexer prefetch and shared Arc read-freshness rules let fast blocks advance during RPC requests. Four focused real-PostgreSQL batch/reorg tests, thirty API fixture checks, thirty-six affected SDK checks and shared/SDK/API/indexer typechecks passed. No broad mathematical/release campaign ran. [Initial indexer evidence](apps/indexer/test/batch-throughput.md), [API evidence](test/evidence/arc-integration/index-freshness.md); the final 64-block worker and actual catch-up are recorded above.

- Owner explicitly authorized upstream Aqua self-deployment on Arc Testnet. Added a separate source-authenticated schema-2 plan and preserved the historical existing-registry plan. The new 12-step plan deploys unchanged AquaRouter first, preserves its helper owner as the user wallet, deploys the existing Orbital graph, and renounces the custom router. Source, constructor simulation, nonce, gas, canonical receipts and final bindings remain checked; canonical-address absence and sponsor acceptance do not block this mode. No application signing key was introduced.

- Historical self-deployment preparation: pinned AquaRouter compilation, deployment/server/browser-script syntax, HTTP status and the first real Arc constructor simulation passed. Plan `be861a38e9ec7fdf9a08a8e3fb46fdb1bce0d85f87aef582b0911961019a4ad1` then had zero confirmations; step one had a reviewed maximum gas budget of about 0.044006 testnet USDC. The operator page awaited twelve signatures at that point; all twelve subsequently succeeded as recorded above. [Unchanged preparation evidence](test/evidence/self-arc-integration.md).

- Owner clarified 1inch scope: custom SwapVM opcodes/redeployments and local-fork token-transfer demonstrations are expressly allowed. Orbital's architecture matches that direction; an official Aqua deployment on Arc is not a stated prerequisite for the 1inch demo. Corrected the master/runbooks to separate that demonstration from the still-unverified Arc deployment and live Privy flow. Existing real local receipts use a fresh Anvil chain with upstream Aqua source; a canonical public-network fork demonstration has not been recorded. This clarification changes no contract, receipt, qualification flag or release status.

- Reviewed the owner-supplied upstream `DEPLOY.md` through its actual script. It performs ordinary CREATE of `AquaRouter(owner)` and does not reproduce the canonical registry address from this wallet. The canonical registry instead came through an owner-restricted CREATE3 factory; its public salt is available, but that exact factory is absent on Arc and was created from another deployer's nonce zero. Canonical extension needs the original deployment authority. The earlier sponsor-evidence gate for self-deployment was superseded by the owner's explicit project-deployment authorization above. No transaction was sent during that investigation. [Concrete factory investigation](docs/ARC_DEPLOYMENT_STATUS.md).

- September 9 remaining integration: added user-triggered Privy embedded-wallet creation, selection after the requested connector becomes available and recoverable onboarding errors. Existing-wallet and credential-free local fixture profiles remain supported. The frontend typecheck passes; live creation, reconnection and wallet financial receipts remain unverified. [Integration behavior and dashboard setup](docs/PRIVY_RESEARCH.md).

- Added the separate `pnpm dev:arc` profile on web 3002/API 3003, using real Privy and no fixture wallet. It starts no Docker services and leaves financial reads unavailable and the indexer off until an authenticated Arc deployment is present. The page and health checks return HTTP 200; readiness correctly returns `503 DEPLOYMENT_UNAVAILABLE`. The public Privy email/wallet modal renders without authentication. [Arc demo runbook](docs/ARC_DEMO.md), [browser observation](test/evidence/arc-integration/privy-login.json).

- Earlier existing-registry checkpoint: implemented Arc deployment inspection, unsigned preparation, status, receipt verification and the reviewed browser utility on port 3100. Its eleven-transaction plan contains six libraries, two demo tokens, router, payments and ownership renunciation; zero transactions were broadcast or confirmed. That historical mode remains blocked by absent authenticated Aqua deployment evidence; the new self-deployment mode above is ready. Public-address balance/nonce observations are not proof of wallet control or a sufficient total gas budget. [Deployment runbook](docs/ARC_DEPLOYMENT.md).

- Fixed unhandled idle/checked-out PostgreSQL connection errors that could terminate the local runner. Shared pools handle connection loss, the indexer retries transient dependencies with sanitized error codes, and the API retains bounded timeouts and unavailable readiness during failure. One private TCP-proxy fault test verifies actual PostgreSQL disconnect, rejection and new-connection recovery without stopping shared services. DB/indexer/API typechecks pass. [Focused evidence and test limits](apps/api/test/database-recovery.md).

- September 9 frontend redesign: the owner supplied the Orbital video and SVG logo, superseding the interim monochrome direction before release. Implemented a matching deep blue identity, local fonts, diagram framing, optional video playback and focused swap/liquidity/payment presentation. Financial controllers and signing ports remain unchanged. Final build and focused desktop/mobile, video, keyboard and local quote/review checks pass. A mobile overflow was fixed; transient local RPC/database failures and the successful quote recheck are retained in the [light frontend checkpoint](test/evidence/frontend-design.md). Earlier monochrome screenshots are not final-design evidence.

- Local setup now compiles application artifacts without compiling the full test suite, verifies/reuses the deployed graph, migrates and seeds idempotently. Repaired interrupted Anvil persistence with atomic snapshots; retained complete accounts/blocks/transactions and reverified their identities. The app runner starts API/indexer/web together and stops only its owned processes. [Runbook](docs/LOCAL_DEMO.md).

- Connected the persistent local contract graph to the API, indexer and browser. Added idempotent deployment verification, local setup/seed/demo commands and an explicit loopback-only Anvil wallet fixture with per-transaction confirmation. Live Privy and Arc verification remain separate.
- Added the three quantized publication presets, bounded approvals, ship/activate reviews and saved-draft recovery. Six 10/100-unit configurations match the deployed initializer; independent Decimal cap/support checks are stable at 110/160 digits. This bounded check does not close general mathematical obligations.
- Added merchant invoice history, reviewed demo funding, confirmed-index quote retries, a wallet initialization retry control, and same-action replacement identity checks. Incomplete Aqua receipts are read from existing canonical raw history and exposed separately without inventing configuration parameters.
- The real browser executed a 1 USDC swap and paid a 5 USDC invoice with 5.019531 oUSD6, yielding 5.015490 USDC and refunding 0.015490 USDC. Recipients received 4.5 and 0.5 USDC. Demo funding also produced mined receipts. Broad testing remains deferred at the owner’s request.

- Implemented strategy owner approval updates and separate retirement/docking reviews with canonical state rechecks and per-hash receipt recovery. The SDK smoke covers three synthetic actions and the web build passes; actual UI-to-contract lifecycle execution remains deferred until the persistent local deployment is connected. [Lightweight checkpoint](test/evidence/strategy-admin.md).
- Owner priority update: finish remaining workflows and end-to-end integration first; keep immediate checks to builds/types and focused smoke checks. Broader testing and optimization are deferred. The in-flight broad strategy browser rerun was intentionally stopped; its checkpoint remains unaccepted, with already completed SDK/shared results retained.
- Implemented liquidity listing/detail with wallet-scoped pagination and validated per-strategy inventory, fees, tick classification and receipt reads. Six focused browser cases and 129 full SDK checks pass. The broad browser rerun was stopped at owner request; publication, retirement/docking, unactivated shipment indexing and the persistent demo remain outstanding. [Read workflow evidence](test/evidence/strategy-read.md).
- Started the accepted docs-then-build plan. Existing repository contained only specifications and research notes; Git working tree was clean.
- Retained authenticated paper HTML locally with source hashes and mathematical extraction metadata. Added explicit support minimizers, numerical dual/primal witness checks, precision-stable fixtures, and both-root crossing diagnostics.
- Preserved two counterexamples: flooring can violate reconstructed boundary prices; a proposed vertical slack-release path can leave/reenter the reconstruction sheet between otherwise valid checks. These block acceptance of that proposed solver path, not evidence of a deployed exploit.
- Implemented 512-bit primitives, directed tick coefficients, endpoint certificates, and the invoice adapter. The endpoint library is explicitly not a complete swap solver.
- Modified only SwapVM.sol in the retained fork; default pair resolution and settlement code remain intact. Tests use official Aqua and a named fixed-output probe, not an Orbital execution claim.
- Built the responsive Next frontend; corrected a 320px wizard overflow. Privy follows its documented wagmi provider integration with explicit active-wallet selection. Presentation does not build swap calldata.
- Retained the current frontend template following the latest user instruction. Added canonical SDK transaction builders and immutable review execution without changing presentation.
- Proved and implemented finite vertical-segment checks for both hidden variance and boundary-price failures. Added exact fractional inward seams with both one-sided reconstructions and a strict eight-segment/seven-seam bound. Clarified MATH-6: per-tick baskets coincide on the frontier; slack seam reclassification preserves their aggregate and maker ownership.
- Added the exact all-interior sphere output primitive using actual starting slack. Its raw-floor/radial-slack proof is local to that primitive; it is not a complete curve engine.
- Implemented atomic bounded reorg recovery, durable resync status, automatic raw replay and guarded API readiness/SSE. A recently written but far-behind cursor cannot be labeled ready. Financial APIs remain unavailable pending the actual engine and materialized data.
- Added signed wide intervals and optimized exact wide division/square roots against the retained slower algorithms. Exact square postconditions remain mandatory. Measured helper costs fell substantially; no complete-swap gas claim follows.
- Implemented fixed-partition original-radical/normal enclosures and both exact-frontier roots, with explicit uncertainty and per-tick physical checks. Independent 110/160-digit vector/basket goldens cover two, three and eight tokens; proof coordinates retain the original stored domain.
- Implemented a global support-gap certificate for output shortfall. Its support branch is selected independently from proposed prices for each tick. Retained a counterexample showing that copying the stored partition can understate the possible output.
- Implemented maker activation/retirement, directed initial principal, all-token wide Aqua backing and guarded lifecycle reads. An explicit size check caught an oversized router; immutable linked libraries brought the lifecycle checkpoint within EIP-170 without changing compiler pins.
- Added actual lifecycle ABI generation and resumable SDK publication/retirement plans. Completed ship/activation/dock steps are preserved, and every future signature still requires refreshed observations and review.
- Added a fixed-partition bracket/refinement primitive with full-interval domain checks and a shared 160-step budget. Cached untouched moments remove repeated midpoint price reconstruction; uncertainty preserves the last certified bracket.
- Proved a finite certificate for exact-frontier segments, including a separate discriminant condition when passing through a traded-reserve equality. Ordinary rounded endpoint checks are not substituted for identified exact roots.
- Independently checked initialization against 12 paper-derived fixtures at 110/160 digits, including mixed decimals, minimum cap width, eight ticks and near-maximum radii. Production code was not changed to make these fixtures pass.
- Proved the all-interior actual-state path, including initial slack release and final raw payout, then integrated it into both custom instructions. All six pairs execute on the real router and official Aqua; a real local swap funds a 5 USDC invoice with 90/10 recipients. Mixed states retain an explicit traversal failure.
- Added exact pair-token deltas for router/maker/taker/recipient, isolated prior donations, cleared Aqua approvals before final checks, and checked all-token backing. Canonical swap events are emitted after settlement; failing transfers or thresholds revert principal, fees and version together.
- Moved activation/trade validation into the existing linked storage library to leave room for execution. The integrated router is 21,919 runtime bytes; every linked library also fits EIP-170. The graph now contains four immutable libraries.
- Added deployment-scoped lifecycle/invoice snapshots and canonical SQL views, with atomic reorg restoration. API readiness now requires the matching fresh canonical projection, not just raw ingestion.
- Verified six mined rollback cases against the real router, with empty receipt logs and unchanged financial state; a repaired swap-funded invoice then succeeds. The disposable receipt runner uses local unlocked Anvil accounts and standard EIP-170 limits.
- Added the fixed-amount demo-token faucet and reviewed SDK self-mint plan. Both token precisions enforce 1,000 units per address every 24 hours on local/Arc test networks; the SDK rejects settlement USDC, unknown tokens, early claims and altered reviews. No frontend redesign or live mint is claimed.
- Added a fixed-input frontier endpoint coupler. A certified original-radical bracket identifies the ideal root; one conservative raw floor and extended same-partition domain checks bound output shortfall and radial slack. Independent supporting-basket goldens cover two, three and eight tokens. This endpoint result does not authorize mixed traversal; the initial connection and ordered crossing path remain separate.
- Added canonical swap receipts, raw token-pair sums and separate chronological backfill coverage. Retained and fixed large-value SQL fee/invoice division and stale-source backfill counterexamples. Applied additive migration 0004 locally; historical 0003 remains unchanged.
- Added GRID-endpoint inward release with exact one-sided seams and preserved legacy behavior. Added a complete conservative event list for strict interior input positions, including hidden double crossings and a 14-event eight-tick fixture. Their composed certificate is implemented separately; production mixed swaps remain unfinished.
- Added read-only receipt metrics with complete historical coverage, exact token-unit totals, canonical RPC dates, stale labels and repeatable database snapshots. Incomplete coverage/RPC failures return unavailable without invented totals. An independent review found and reproduced a slow-read timestamp bug; its fix and regression pass.
- Composed initial GRID release, identified frontier roots, both-root schedules and conditional arc/turn certificates under one 160-refinement/16-crossing ledger and one final payout. Eighteen contract tests and fifteen independent reference tests bind nine successful cases, including real two-/four-crossing paths. Two independent conditional reviews found no concrete soundness defect. Equality, discovery/precision liveness, final repartition and production gas remain open; this helper is not integrated into the router.
- Added canonical invoice detail/list endpoints, exact recipient floor/remainder amounts, status/payment receipt provenance and historical pagination that survives normal tip advancement. Orphaned pins require a fresh listing; incomplete/current-marker/RPC failures remain unavailable. All 53 API tests pass. Public invoice detail rendering now uses the existing frontend template and a separately validated SDK read boundary. Independent reviews reproduced terminal-log provenance, deployment-refresh and stalled-request defects; fixes and regressions pass.
- Added exact payout-targeted root stopping and authenticated continuation from retained bounds. Ordinary solver APIs and all final domain, rounding and path predicates are preserved. A near-event fixture requires both phases and uses 125 of the shared 160 refinements. Independent reference checks retain the feasible seed-discovery failure. Measured n3/n8 helper-call gas falls to 8.95M/16.74M; these exclude complete settlement, and n8 remains above the provisional deployment budget. See [payout evidence](test/evidence/payout-refinement.md).
- Added registered-strategy listings and canonical block-pinned financial detail. Immutable configuration, source snapshots and swap-fee receipts are checked against three exact contract getters, with full integer principal/backing/availability coherence. Historical pages retain their original canonical pin; partial reads, reorgs and inconsistent observations expose no balances. Transport reserves the existing shared eight-request capacity, bounds the six-read detail group to eight seconds and rejects malformed or oversized responses. The full API suite passes 81 tests; quote execution and incomplete unactivated shipment coverage remain outstanding. See [strategy evidence](test/evidence/strategy-reads.md).
- Promoted the reviewed math split into two pure linked libraries, preserving every numerical body and checking actual external calls. Added a narrow two-token high-sheet proposal after diagnosing a feasible bracket-discovery failure; the recovered path matches the independent raw-output golden and uses the shared refinement ledger. Current runtime sizes are 22,253/18,812 bytes for composition/endpoint. Cold n8 helper consumption is about 11.58M gas; complete router transaction requirements remain unverified. See [linked promotion](test/evidence/linked-math-promotion.md) and [seed diagnosis/promotion](test/evidence/seed-proposal.md).
- Added a bounded whole-size routing core: at most 200 canonical candidate records, 32 inspected candidates, exact caller-sensitive static-call plans, live-output checks, deterministic ranking and three alternatives. Payment proposals retain payer separately from adapter caller/recipient. Nine new tests bring the API suite to 90; canonical transport/service orchestration is in progress and public quote endpoints remain disabled. See [routing evidence](test/evidence/route-selection.md).
- Promoted the independently reviewed final-retention repartition proof into the pure endpoint/composition libraries. Six reference tests now bind the exact-seam and seven-seam fixtures to main Solidity literals; 148 affected contract tests pass. Actual and ideal prefixes remain distinct, and tagged retention seams consume the same crossing budget. Router integration is next; [promotion evidence](test/evidence/outward-retention-promotion.md).
- Added a derived initialized three-token history: one outward trade, then an inward/outward trade from the preceding actual raw payout. Four tests pass with identical 110/160-digit results. A retained numerical Newton discovery failure is resolved by an independently checked price-space proposal, without changing default oracle fixtures. [Reference evidence](test/evidence/reachable-traversal.md).
- Completed internal canonical quote orchestration and native bounded RPC batches. Final identity/database/time checks reject partial or stale results; cancelled responses retain admission until outstanding SQL transactions release their clients. Thirty new checks bring the full API suite to 120. Public swap quote observations are next; financial execution remains disabled. [Service evidence](test/evidence/quote-service.md).
- Added a strict SDK swap-observation decoder; nine new checks bring the SDK suite to 72. It binds configuration/order hashes, every retained route, exact financial limits and server/client freshness, while retaining `financialExecutionEnabled:false`. [Decoder evidence](test/evidence/quote-read-sdk.md).
- Completed a frozen all-interior stateful campaign: 256 runs at depth 128, 32,768 successful handler calls, zero unexpected reverts/discards. Every run covers all six pairs, quotes, donations, explicitly failed transfer recovery and final retire/dock with exact ghost accounting. This is one maker/strategy and a historical source graph, not the full release matrix. [Campaign evidence](test/evidence/interior-stateful.md).
- Resolved the initialized reverse trade's conservative schedule deferral with a separate strict negative-price exclusion certificate. Both initialized pure compositions match their independent raw-output goldens; 167 affected contract tests pass. Exact zero remains unknown, and mere failure of physical certification cannot discard an event. [Proof and results](test/evidence/negative-price.md).
- Completed the full unchanged upstream regression: stock and minimal fork each pass 807 tests across the same 103 suites, with zero failures/skips, identical passing test identities and unchanged pre/post source inputs. The preliminary run invalidated by a runner change remains separate. [Upstream evidence](test/evidence/upstream-regression.md).
- Exposed canonical swap observations through both public HTTP aliases with strict request/response validation, bounded shared rate limits, cancellation and final deployment/freshness rechecks. All 130 API tests pass, including SDK decoder compatibility. Observations retain `financialExecutionEnabled:false`; payment quotes and wallet execution remain separate. [HTTP evidence](test/evidence/swap-quote-http.md).
- Integrated the certified mixed path through real Router/Aqua settlement. Both initialized reference outputs, ordered crossings, actual canonical metadata, fee separation and failure→other-maker→retry cleanup pass. The complete default-profile checkpoint passes 388 tests across 43 suites with unchanged inputs. [Integration evidence](test/evidence/mixed-execution.md), [full run](test/evidence/contracts-current.json).
- Added validated public swap observations to the existing frontend template. Eight new browser checks bring the full suite to 19; type checking, production build and mobile/desktop review pass. Wallet, chain and input changes hide old results; expiry/cancellation/failed refresh expose no stale amounts. No transaction submission is enabled. [UI evidence](test/evidence/swap-observation-ui.md).
- Created commit `b57da28` for the verified upstream compatibility checkpoint following the user's authorization to commit. Pushing remains with the user. Exact source/evidence bytes are preserved by Git attributes.
- Created protocol checkpoint `1e4f1bd`: certified mixed execution, independent reference, conditional proof audits and frozen local campaigns. Unresolved liveness and release requirements remain explicit.
- Created application/tooling checkpoint `72aca1f`: canonical financial observations, validated SDK/controller boundaries, current-template browser coverage and authenticated disposable deployments. No push was performed.
- Reconciled the proof page with current evidence. The old indexer transcript contained only the six raw-reorg tests, while later 31-test verification was recorded in its detailed report. Retained that old transcript separately and reran the current indexer: 31/31 pass in 19.703 seconds, with no failures, skips or cancellations.
- Verified a real mixed swap-funded 150 USDC invoice with a 135/15 split and 14.721797 USDC refund. Two isolated contract tests pass; a mined second-recipient failure has status 0, no logs and identical financial state, then identical calldata succeeds after restoring the recipient. This is additional evidence after the 388-test checkpoint, not a full PAY campaign. [Receipt and source evidence](test/evidence/mixed-invoice.md).
- Generated a precision-stable independent mixed pilot: 32 configurations, four actual-payout net-input actions each, no exclusions or input halvings. The initial 127/128 result exposed an n3/eight-tick reversal seed inside the excluded radial sheet. An independently reviewed exact lower-sheet proposal recovers that case while retaining all root/path/payout gates. Fresh linked observations pass 128/128, with 42 inward and 112 outward transitions. The original failure and source graph remain archived. [Pilot and promotion evidence](test/evidence/mixed-pilot.md).
- Implemented the bounded payment search and invoice/funding foundation: strict request, compiled read ABI, one pinned 7/8-member context batch, exact indexed/onchain invoice matching and integer funding bounds. The independently reviewed search retains a sufficient observation without certifying a minimum; 16 focused tests include partial-oracle and shared-memory counterexamples. Internal payment minimum/expiry protection now survives a slow final database read. Full regressions pass 168 API / 79 SDK / 5 shared tests and workspace types. The public payment endpoint, integrated final checks and wallet execution remain unfinished. [Evidence and retained failures](test/evidence/payment-foundation.md).

- Added canonical payment service/runtime composition and both public quote aliases. Exact unsigned plans are independently reconstructed by the SDK before publication; final invoice/funding/source/identity/time changes discard stale results. Swap/payment requests share admission, weighted RPC capacity and IP rate limits. Focused tests cover the 44-batch boundary, real PostgreSQL/native-RPC observations, manifest timeouts, socket cancellation and recovery. The isolated service review is conditional and excludes the new public decoder/HTTP layer. [Evidence and retained failures](test/evidence/payment-endpoint.md).

- Added a runtime-owned five-second cache for complete router quote batches, keyed by exact deployment, pin, order versions, intent/calls and inspected state. Identity, invoice, funding and all source/final checks stay fresh. Cache hits preserve search limits and are counted separately from native work; independent SDK subset enumeration covers every supported batch shape. Concurrent callers retain independent cancellation. [Cache evidence and retained diagnostics](test/evidence/quote-cache.md).

- Implemented direct-USDC and swap-funded invoice payment reviews through the shared wallet module. Exact approval/payment remain separate signatures. Live invoice/funding checks, simulation, reviewed gas limits and shared Arc USDC funding are mandatory. Saved hashes survive interruption/account changes. Reproduced and fixed review revival after A-to-B-to-A and incomplete receipt-hash binding. [Workflow evidence and retained failures](test/evidence/payment-flow.md).

- Implemented standalone swap review, exact approval, live strategy/funding checks and canonical settlement-event decoding. Per-hash recovery survives reload; altered saved approvals are rejected. The full frozen checkpoint passed 110 SDK, 7 shared and 44 browser/storage checks, production build and workspace types with 721 unchanged inputs. [Workflow evidence](test/evidence/swap-flow.md).

- Implemented the documented swap defaults, persisted settings, debounced/periodic quotes, canonical balance display and gas-aware Max. Unknown future approval/swap gas stays explicit; no gas is deducted from other assets. Network changes use the active connector. The complete frozen checkpoint passed 114 SDK, 7 shared and 56 browser/recovery checks, production build and workspace types with 731 unchanged inputs. [Control evidence](test/evidence/swap-controls.md).

- Measured the three expensive n8/eight-tick cases against independent raw-output/reserve/transition goldens. All six diagnostic tests pass. A batch-discovery candidate passed focused equivalence tests but exceeded EIP-170 and was rejected; production sources were restored exactly. [Gas diagnosis and retained candidate](test/evidence/pilot-gas.md).
- Updated delivery priority: finish the missing functional workflows across protocol, backend, SDK and application before further optimization or visual redesign. Gas and mathematical release obligations remain requirements before deployment.

- Implemented invoice creation with one to three exact recipients, merchant cancellation, compiled nonce reads and canonical event-based identifiers. Shared wallet reviews remain single-use with bounded gas and per-hash recovery. All application checks pass, including 16 new browser/storage checks. [Invoice administration evidence and retained failures](test/evidence/invoice-admin.md).

## Latest verification

- **Quote timing checkpoint (2026-09-09):** nineteen focused SDK/refresh checks and four Chromium checks passed. Three live Arc quotes through the application retry helper returned `QUOTE_OBSERVED`. Readability, expiry, background recovery, invoice polling and exact local payment reconstruction are covered; no actual user signature or deployment change was performed. [Evidence and initial fixture failures](test/evidence/quote-refresh/README.md).

- **First live strategy checkpoint (2026-09-09):** the focused [receipt/backing/quote check](test/evidence/arc-integration/first-strategy.md) passed. No new chain write or broad test campaign was performed. This supersedes the earlier missing-liquidity checkpoint; trader swap/payment and Privy wallet verification remain separate.

- **Remaining-integration checkpoint (2026-09-09):** verification is limited to focused types, database recovery, deployment/profile HTTP checks and a public Privy modal observation. The Privy frontend typecheck and DB/indexer/API typechecks pass; one real PostgreSQL fault-injection test passes, retaining its initial fixture-timeout diagnostic separately. Arc page/health return HTTP 200, readiness returns `503 DEPLOYMENT_UNAVAILABLE`, and no database/indexer starts for the unconfigured target. The public email/wallet modal renders without SDK errors, authentication or signatures. The prepared eleven-transaction plan has zero confirmed transactions and no pending hash; no live Privy swap, invoice or Arc deployment is claimed. Broad release and mathematical campaigns are deferred at the user's request, not waived. [Database evidence](apps/api/test/database-recovery.md), [public-modal observation](test/evidence/arc-integration/privy-login.json), [Arc preparation](docs/ARC_DEPLOYMENT.md).

- **Retained local integration checkpoint (2026-09-09):** twelve deployed local contracts, fourteen retained browser transaction receipts and six API observations verified after a graceful restart. Browser smoke includes publication reload recovery, separate incomplete-shipment indexing, activate/retire/dock, swap, swap-funded invoice, funding and invoice creation/cancellation. Workspace types and production frontend build passed for that checkpoint. Full regression counts below describe earlier frozen checkpoints, not reruns of the remaining-integration increment. [Scope, failures and artifacts](test/evidence/local-integration.md).

- Latest liquidity read checkpoint: production build and workspace types pass with 759 unchanged authored inputs. Six focused browser cases and the subsequent 129-SDK/7-shared run are recorded separately; broad browser testing was intentionally deferred. The production preview serves both liquidity routes and referenced assets. [Scope](test/evidence/strategy-read.md).
- Solidity: full checkpoint is **398 tests across 46 suites**, seed `0x20260908`, default fuzz 256 and invariant 32×64 with unexpected reverts forbidden. All source/configuration inputs stayed unchanged; elapsed time 511.885 seconds including compilation. The earlier verified 388-test checkpoint and preliminary invalidated runner remain historical. Router/Storage/Composition/Endpoint runtimes are 22,221/23,240/23,138/22,826 bytes. Fresh actual cold external mixed calls use 5,167,894/5,834,667 gas, excluding transaction intrinsic/minimum-forwarding requirements. The older 8×8 activation observation is 3,791,206 gas on its archived graph. None establishes worst-range or Arc acceptance.
- Reference: **129 tests passed in 53.508 seconds**, with unchanged input hashes and a valid computation manifest. New coverage includes mixed-pilot consistency/actual baskets, reversal literal binding, and an exact pair-difference/bisection oracle: 47 Solidity proposal fixtures plus 625 exhaustive small oracle controls. The separate full pilot generator produced identical 110/160-digit corpora in 560.278 seconds. Earlier scalar, root, initializer, both-root, price, slack and retention counterexamples remain included. Numerical/reference histories are not onchain receipts or universal proofs.
- SDK: **129 tests pass**; complete shared suite: **7 pass**. Six strategy read checks now cover deployment/cursor/receipt identity and exact principal/fee/backing observations. [Current SDK checkpoint](test/evidence/strategy-read/final/checkpoint.json) retains the intentionally interrupted browser run separately. Nine new invoice administration checks cover immutable reviews, nonce/ownership/status changes, gas and expiry, exact receipt provenance and authoritative creation IDs. The five-getter read ABI includes the existing merchant nonce getter with an independently checked selector. [Checkpoint and scope](test/evidence/invoice-admin.md).
- API: **210 tests passed in 289.968 seconds**, using serialized files and isolated PostgreSQL schemas. The current compiled read ABI is covered by the unchanged API regression set. No API implementation changed in the invoice administration increment. PostgreSQL ingestion/reorg (6) and indexer (31) retain their earlier source checkpoints. [Exact API transcript](test/evidence/invoice-admin/final/api.txt), [original cache coverage and retained timing failures](test/evidence/quote-cache.md).
- Browser: **72 tests pass**, including 14 new invoice creation/cancellation workflows and 2 recovery-record checks, plus all 56 previous checks. Split/remainder reviews, nonce/payment races, active-wallet/network changes, signature rejection, missing/mismatched events, reverts, storage corruption and reload recovery are covered with synthetic fixtures. The 320px review is readable without overflow. [Evidence](test/evidence/invoice-admin.md).
- Public Privy login rendered successfully with the supplied app ID; no authentication or financial-flow qualification is claimed.
- Production frontend build and workspace TypeScript checks pass. The final browser/proof/build/type commands use **749 unchanged authored inputs**; previously passed SDK/API/shared commands are explicitly reused after validating their hashes and unchanged inputs. The only intervening source changes were a browser negative-test typing correction and the completion runner. Next-generated declarations/caches and external runtime state are excluded. [Exact completion record](test/evidence/invoice-admin/completed/checkpoint.json).
- Docker image build and API/web startup checks passed; see test/evidence/docker-build.txt and docker-runtime.json. This is a development scaffold check, not an integrated protocol deployment.
- The current frontend template is retained as requested. Its browser/build observations are recorded separately from backend and math verification.
- The full contract run includes 20 signed/interval, 26 wide-oracle, 9 dual-certificate, 17 scalar/event, 12 turn, 19 bracket, 15 interior-path, 22 lifecycle and 6 independent initialization tests. A separate dual-helper run used 2,048 seeded cases. Root test costs dropped from 51.7–207.7M to 5.9–8.0M gas in the measured fixtures; these include setup and do not establish complete-swap gas acceptance.
- Stateful accounting: the separate frozen all-interior run passed 256×128 calls with `fail_on_revert=true`, authenticated source copies and end-of-run closure. Per-run asserted totals are 128 swaps, 224 quotes, 32 expected failed transfers/recoveries, 32 router donations, 32 Aqua pushes and all six pairs. A separate current mixed-capable graph replay passed 64 shared-inventory sequences across three strategies/two makers, totaling 2,240 swaps, 2,752 quotes and 192 retire/dock operations. Those trades remain all-interior; mixed handler histories and the full release campaign remain open. [Shared evidence](test/evidence/shared-inventory-mixed.md).
- Disposable local deployment: the authenticated six-library graph deployed 12 contracts and verified 16 receipts, exact linked runtime code, constructor/domain bindings and faucet state. All nine local graph/Anvil checks and the actual deployment test pass. The owned chain was closed; its candidate remains `verified:false` and financial execution disabled. This is not a persistent demo, Arc deployment or live Privy verification. [Deployment evidence](test/evidence/local-deployment.md).
- Mixed invoice: 2/2 isolated tests and 30 mined setup/action transactions pass with unchanged inputs. The failed second split consumes 5,366,757 receipt gas and reverts every observed financial field; restored identical calldata consumes 4,973,226 and records the exact swap then payment. Receipt gas includes intrinsic gas. This isolated compilation has its own authenticated runtime identity and is not an Arc measurement. [Evidence](test/evidence/mixed-invoice.md).
- Mixed pilot: 128/128 fresh matches (68 interior, 60 mixed), 32/32 complete initialized net-input histories; two linked libraries and a diagnostic harness authenticated on an owned disposable chain, then closed. No initial-release/final-retention transitions occur in this matrix. The n8/eight-tick concentrated step2/step3 diagnostic calls use **18,368,590/17,623,174 gas**, already above the provisional Arc cap before complete settlement. General gas acceptance remains blocked. [Frozen source and run evidence](test/evidence/mixed-pilot.md).
- Upstream compatibility: 807/807 unchanged tests pass on both stock and fork, 103 suites each, same qualified test set. Inputs and transcript hashes were verified. This tests the retained assertions, not exhaustive equivalence for every possible input or gas equality.

## Next concrete work

**Latest checkpoint:** the first trader swap and **0.5-USDC swap-funded invoice** are complete, with retained receipts. Invoice input now calculates automatically; use a new unpaid invoice to exercise the updated UI, because the existing invoice is already paid. Next verify authenticated wallet kind/creation/reconnection and associate actual financial receipts with a Privy embedded wallet for sponsor evidence. Chain data alone cannot establish wallet provider.

The Arc application at http://localhost:3002 has its verified deployment and active strategy. Keep the browser origin already used for wallet permissions and saved work. Maker A and trader B remain distinct for swap execution; any new trader needs its own testnet USDC for gas and chosen input asset. The user chooses the wallet. Do not repeat deployment, funding or strategy publication that is already complete. The separate local application remains at http://127.0.0.1:3000.

Deferred numerical, security, regression and release campaigns remain required; recorded local evidence cannot close those gates. The supplied-media frontend is the editable design baseline; pushing remains with the user. Follow [Arc demo setup](docs/ARC_DEMO.md) and [deployment preparation/recovery](docs/ARC_DEPLOYMENT.md) for the remaining integration.

Detailed commands and outcomes belong in [the evidence index](test/evidence/INDEX.md).

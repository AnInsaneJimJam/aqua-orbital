--- a/packages/contracts/src/libraries/CurveEvaluation.sol
+++ b/packages/contracts/src/libraries/CurveEvaluation.sol
@@ -83,6 +83,23 @@
         }
         if(totalRadius>=(uint256(1)<<160))revert InvalidContext();
     }
+    /// @notice Advance a prepared context by exactly one sorted ordinary tick.
+    /// @dev Internal cache operation: ctx must be the unmodified prepare result
+    /// (or this operation's previous result) for these same immutable ticks.
+    /// prepare already checked every key/radius and the total bound. Recompute
+    /// the crossed coefficient, preserving per-tick rounding BEFORE GRID lift.
+    /// Mutates ctx in memory; callers must not retain it as an earlier prefix.
+    function advance(Context memory ctx,M.Tick[] memory ticks) internal pure returns(Context memory){
+        uint256 count=ctx.boundaryCount;
+        if(count+1>=ticks.length)revert InvalidContext();
+        M.Tick memory tick=ticks[count];G.Coefficients memory c=G.coefficients(ctx.n,tick.key);
+        ctx.boundaryCount=uint8(count+1);ctx.nextKey=ticks[count+1].key;
+        ctx.radius-=uint256(tick.radius)*GRID;ctx.axial+=uint256(tick.radius)*tick.key;
+        ctx.sigmaLo+=W.mulDiv(tick.radius,c.sigmaLo,Q,false)*GRID;
+        ctx.sigmaHi+=W.mulDiv(tick.radius,c.sigmaHi,Q,true)*GRID;
+        ctx.outerKey=tick.key;ctx.outerSigmaHi=c.sigmaHi;
+        return ctx;
+    }
     /// @notice Encloses the original radical F-R^2 and every normal component.
     /// @dev ctx must be returned by prepare without modification. A non-Evaluated
     /// result has no valid radical/normal bounds. This method does not certify a
--- a/packages/contracts/src/libraries/FrontierEndpoint.sol
+++ b/packages/contracts/src/libraries/FrontierEndpoint.sol
@@ -59,6 +59,21 @@
         if(result.status==Status.UncertifiedStart)return result;
         C.Context memory ctx;(result.root,ctx)=_identify(fixedPoint,ticks,output,count,budget,0);
         return _finalize(x,ticks,output,count,fixedPoint,ctx,result);
+    }
+    /// @notice First identified final prefix, using zero midpoint refinements.
+    /// @dev Identity only, not a payout or path certificate. Validate the start
+    /// once, then advance internally regenerated contexts in ascending order.
+    /// No caller-supplied context, prior validation flag or retained witness is
+    /// accepted. The final payout and path still require their ordinary checks.
+    function discoverFinal(uint256[] memory x,M.Tick[] memory ticks,uint8[] memory decimals,uint8 input,uint8 output,uint256 rawNetInput) public pure returns(Identity memory root){
+        (Result memory prepared,uint256[] memory fixedPoint)=_prepareInput(x,ticks,decimals,input,output,rawNetInput,0);
+        if(prepared.status==Status.UncertifiedStart)return root;
+        C.Context memory ctx=C.prepare(uint8(x.length),ticks,0);
+        for(uint256 count;count<ticks.length;count++){
+            Identity memory candidate=_identifyPrepared(fixedPoint,output,0,0,ctx);
+            if(candidate.identified)return candidate;
+            if(count+1<ticks.length)ctx=C.advance(ctx,ticks);
+        }
     }
     function _prepareInput(uint256[] memory x,M.Tick[] memory ticks,uint8[] memory decimals,uint8 input,uint8 output,uint256 rawNetInput,uint8 budget) private pure returns(Result memory result,uint256[] memory fixedPoint){
         uint256 n=x.length;
@@ -130,9 +145,13 @@
         (root,)=_identify(x,ticks,output,count,budget,0);
     }
     function _identify(uint256[] memory fixedPoint,M.Tick[] memory ticks,uint8 output,uint8 count,uint8 budget,uint256 payoutQuantum) private pure returns(Identity memory root,C.Context memory ctx){
+        ctx=C.prepare(uint8(fixedPoint.length),ticks,count);
+        root=_identifyPrepared(fixedPoint,output,budget,payoutQuantum,ctx);
+    }
+    function _identifyPrepared(uint256[] memory fixedPoint,uint8 output,uint8 budget,uint256 payoutQuantum,C.Context memory ctx) private pure returns(Identity memory root){
         uint256 n=fixedPoint.length;
+        uint8 count=ctx.boundaryCount;
         root.boundaryCount=count;root.bracket.remaining=budget;
-        ctx=C.prepare(uint8(n),ticks,count);
         uint256[] memory high=new uint256[](n);uint256 fixedSum;
         for(uint256 i;i<n;i++){high[i]=fixedPoint[i]*GRID;if(i!=output)fixedSum+=high[i];}
         // The seam sums are exact GRID numerators, not rounded original lengths.
@@ -143,12 +162,12 @@
         }
         uint256 upperKey=ctx.nextKey==type(uint64).max?(n-1)*GRID:ctx.nextKey;
         uint256 upperSum=ctx.axial+(ctx.radius/GRID)*upperKey;
-        if(upperSum<fixedSum)return(root,ctx);
+        if(upperSum<fixedSum)return root;
         if(high[output]>upperSum-fixedSum)high[output]=upperSum-fixedSum;
-        if(high[output]<lower)return(root,ctx);
+        if(high[output]<lower)return root;
         if(n==2&&count!=0&&high[1-output]>high[output]){
             (bool proposal,uint256 clipped)=_capN2High(high[1-output],high[output],ctx.sigmaHi,lower);
-            if(!proposal)return(root,ctx);
+            if(!proposal)return root;
             high[output]=clipped;
         }
         C.Evaluation memory feasible=C.evaluate(high,ctx,output);
@@ -157,10 +176,10 @@
             // One exact variance-based proposal replaces it; no failed sign
             // chooses a root or discards an event. Recheck every certificate.
             (bool proposal,uint256 clipped)=L.cap(high,output,ctx.sigmaHi,lower);
-            if(!proposal)return(root,ctx);
+            if(!proposal)return root;
             high[output]=clipped;feasible=C.evaluate(high,ctx,output);
         }
-        if(!C.certifiesMembership(feasible)||!feasible.strictOutputPrice)return(root,ctx);
+        if(!C.certifiesMembership(feasible)||!feasible.strictOutputPrice)return root;
         // Proposal only. Its strong-convexity implication requires the full
         // domain, so ordinary RootBracket rechecks both signs and every domain
         // condition. Clipping this seed is never treated as a lower-side proof.
--- a/packages/contracts/src/libraries/FrontierComposition.sol
+++ b/packages/contracts/src/libraries/FrontierComposition.sol
@@ -62,12 +62,9 @@
         if(result.releaseCrossings>maxCrossings){result.status=Status.TransitionLimit;return result;}
         result.crossingRemaining=maxCrossings-result.releaseCrossings;
 
-        bool foundFinal;uint8 finalPrefix;
-        for(uint8 count;count<ticks.length;count++){
-            E.Result memory proposal=E.exactInput(x,ticks,decimals,input,output,rawNetInput,count,0);
-            if(proposal.root.identified){foundFinal=true;finalPrefix=count;break;}
-        }
-        if(!foundFinal)return result;
+        E.Identity memory discovered=E.discoverFinal(x,ticks,decimals,input,output,rawNetInput);
+        if(!discovered.identified)return result;
+        uint8 finalPrefix=discovered.boundaryCount;
         F.Result memory schedule=F.enumerate(x,ticks,input,output,netInput,result.initial.boundaryCount,finalPrefix,result.crossingRemaining);
         if(schedule.status==F.Status.CrossingLimit){result.status=Status.TransitionLimit;return result;}
         if(schedule.status!=F.Status.OrderingCertified)return result;
